Você é um assistente de desenvolvimento integrado ao MozHost. O projeto está em Node.js com Express, usa MySQL via ../models/database, e segue o padrão já existente em mozhostAiFunctions.js.
Tarefa: Adicionar 4 novas functions ao arquivo mozhostAiFunctions.js seguindo exatamente o mesmo padrão já existente (declarações + implementações + já integradas no executeFunction):
listar_arquivos(diretorio, container_id) - lista arquivos e pastas de um diretório do container
ler_arquivo(caminho, container_id) - lê o conteúdo de um arquivo
editar_arquivo(caminho, novo_conteudo, container_id) - sobrescreve o conteúdo de um arquivo
executar_comando(comando, container_id) - executa um comando no terminal do container e retorna o output
Segurança obrigatória: Antes de qualquer operação de arquivo ou comando, validar no banco se o container_id pertence ao usuário autenticado. Nunca permitir path traversal (../).
